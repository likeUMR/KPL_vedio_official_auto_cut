# Utility Experiments

This folder keeps one-off analysis helpers that were useful during exploration but are not part of the production pipeline.

- `analyze_scene_durations.py`: summarizes scene duration distributions after scene splitting.

These scripts are archived to keep the root pipeline stage layout focused.
